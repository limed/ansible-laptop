---
- hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - './vars/common.yml'

  roles:
    - role: homebrew
      tags: homebrew
    - role: common
      tags: common
    - role: 'vim'
    - role: 'gpg'
    - role: 'ssh'
    - role: 'docker'
    - role: 'git'
      git_user: limed
      git_email: limed@mozilla.com
      github_username: limed
      github_name: Ed Lim
